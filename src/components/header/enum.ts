enum TagNames {
  DIV = 'div',
  HEADER = 'header',
  NAV = 'nav',
  A = 'a',
  IMG = 'img',
  SPAN = 'span',
}

enum Styles {
  HEADER = 'header-nav',
  CONTAINER = 'container-nav',
  LINK = 'nav-link',
  NAV = 'nav',
  NAV_OPEN = 'nav--open',
  NAV_PRODUCTS = 'nav__products',
  NAV_USER = 'nav__user',
  LOGO_WRAPPER = 'logo-wrapper',
  BURGER = 'burger-icon',
  BURGER_ACTIVE = 'burger-icon--active',
  BURGER_LINE = 'burger-icon__line',
  IMAGE = 'img',
  LOCK_BODY = 'lock',
}

enum Events {
  CLICK = 'click',
}

enum Attributes {
  SRC = 'src',
  HREF = 'href',
}

export { TagNames, Styles, Events, Attributes };
