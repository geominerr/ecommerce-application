enum TagNames {
  DIV = 'div',
  FIELDSET = 'fieldset',
  LEGEND = 'legend',
}

enum Styles {
  FIELDSET = 'fieldset',
  LEGEND = 'legend',
  TITLE = 'profile-title',
  CONTAINER = 'container--profile-page',
}

enum Contents {
  LEGEND = 'Personal information',
}

enum Events {
  INPUT = 'input',
}

export { TagNames, Styles, Contents, Events };
