enum TagNames {
  FIELDSET = 'fieldset',
  BUTTON = 'button',
  DIV = 'div',
}

enum Styles {
  FIELDSET = 'fieldset-passwords',
  FIELDSET_SHOW = 'show',
  LEGEND = 'legend',
  BUTTONS_CONTAINER = 'password-buttons-container',
  BUTTON_CANCEL = 'cancel-button',
  BUTTON_SAVE = 'save-button',
  BUTTONS_HIDE = 'hide',
  BUTTONS_SHOW = 'show',
  HIGHLIGHT = 'highlight',
}

enum Events {
  INPUT = 'input',
}

export { TagNames, Styles, Events };
