import { InputOptions } from './fieldset-interfaces';

const promoOptions: InputOptions = {
  ID: 'promo',
  TYPE: 'promo',
  NAME: 'promo',
  PLACEHOLDER: 'Enter promo code',
  CONTENT_LABEL: 'Promo Code',
};

const OPTIONS: InputOptions[] = [promoOptions];

export { OPTIONS };
