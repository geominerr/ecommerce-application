enum TagNames {
  FIELDSET = 'fieldset',
  LEGEND = 'legend',
}

enum Styles {
  FIELDSET = 'fieldset',
  LEGEND = 'legend',
}

enum Contents {
  LEGEND = 'Personal information',
}

enum Events {
  INPUT = 'input',
}

export { TagNames, Styles, Contents, Events };
