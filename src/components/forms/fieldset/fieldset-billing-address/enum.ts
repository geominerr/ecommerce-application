enum TagNames {
  FIELDSET = 'fieldset',
  LEGEND = 'legend',
}

enum Styles {
  FIELDSET = 'fieldset',
  LEGEND = 'legend',
}

enum Contents {
  LEGEND = 'Billing address',
}

export { TagNames, Styles, Contents };
